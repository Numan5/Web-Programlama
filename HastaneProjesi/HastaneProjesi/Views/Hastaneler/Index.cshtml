@model IEnumerable<HastaneProjesi.Models.Hastaneler>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/Sablon.cshtml";
}

<br />

<table id="example" class="table table-dark">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ad)
            </th>
           
            <th width="100px">Güncelle</th>
           
            <th width="100px">Sİl</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td class="alert-secondary">
                    @Html.DisplayFor(modelItem => item.Ad)
                </td>
                
                <td class="alert-dark">
                    <a asp-action="Edit" class="btn btn-primary " asp-route-id="@item.Id">Güncelle</a>

                </td>
              
                <td class="alert-dark">
                    <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.Id">Sil</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<p>
    <a asp-action="Create" class="btn btn-warning">Hastane Ekle</a>
</p>
